function XO(str) {
  let letterO = 0;
  let letterX = 0;
  
  for ( let letter of str){
    if ( letter === "o" || letter === "O") {
      letterO = letterO + 1
    }else if (letter === "x" || letter === "X")
      letterX = letterX + 1
  } 
  return letterO === letterX
  
